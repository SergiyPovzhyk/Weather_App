<?xml version="1.0" encoding="utf-8"?>
<androidx.constraintlayout.widget.ConstraintLayout
    xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    android:layout_width="match_parent"
    android:layout_height="wrap_content"
    android:background="#31333D">

    <TextView
        android:id="@+id/tv_rv_hour"
        android:layout_width="36dp"
        android:layout_height="16dp"
        app:layout_constraintTop_toTopOf="parent"
        app:layout_constraintStart_toStartOf="parent"
        android:layout_marginTop="24dp"
        android:layout_marginStart="20dp"
        tools:text = "07:00"
        android:textColor="#FFFFFF"
        android:fontFamily="@font/inter_light"
        android:textSize="13sp"
        />

    <TextView
        android:id="@+id/tv_rv_temp"
        android:layout_width="23dp"
        android:layout_height="17dp"
        tools:text = "13"
        app:layout_constraintTop_toTopOf="@+id/tv_rv_hour"
        app:layout_constraintEnd_toStartOf="@+id/tv_rv_degree"
        android:textColor="#FFFFFF"
        android:fontFamily="@font/inter_light"
        android:textSize="14sp"/>
    <ImageView
        android:id="@+id/iv_rv_cloudly"
        android:layout_width="24dp"
        android:layout_height="24dp"
        android:layout_marginEnd="26dp"
        app:layout_constraintTop_toTopOf="@+id/tv_rv_hour"
        app:layout_constraintEnd_toStartOf="@+id/tv_rv_temp"
        tools:src = "@drawable/ic_launcher"/>
    <TextView
        android:id="@+id/tv_rv_degree"
        android:layout_width="11dp"
        android:layout_height="27dp"
        app:layout_constraintTop_toTopOf="@+id/tv_rv_temp"
        app:layout_constraintEnd_toEndOf="parent"
        android:layout_marginEnd="20dp"
        android:text="°"
        android:textColor="#FFFFFF"
        android:fontFamily="@font/inter_light"
        android:textSize="14sp"/>

</androidx.constraintlayout.widget.ConstraintLayout>